// Экранирует спецсимволы регулярных выражений в пользовательском вводе.
// Без этого поисковая строка (?search=...) подставляется в $regex как есть
const escapeRegex = (value = "") => value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");

module.exports = escapeRegex;
